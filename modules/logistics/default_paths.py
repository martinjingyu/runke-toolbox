"""几张跨部门共用的核心表格，生产环境统一放在同一个网络路径下：
\\Rk\\采购订单出货记录\\订单&出货&产品信息
（本机能访问到的 \\rk\\公共文件\\个人\\黄靖禺\\订单&出货&产品信息 是这份数据的测试副本，
文件名跟生产环境一致，开发/测试时可以拿这份看结构，但工具里给的默认路径统一指向上面那个
生产路径）。

只给"只读这几张表、不写回"的工具当默认值用——会往表里写数据的工具（比如
shipment_plan_apply、purchase_order_import）不在这里给默认路径：那些工具每次实际改的是哪个
文件，应该让操作的人自己明确选，不该图省事默认指向真正会被写入的生产文件，写错文件的后果
比多点一次「浏览…」严重得多。

这里的路径只是"预填在输入框里的默认值"，不是写死不能改——输入框本身还是可以手动改/粘贴
别的路径，遇到文件改名、或者需要处理别的批次数据时，照样能换。
"""
from __future__ import annotations

_BASE_DIR = r"\\Rk\采购订单出货记录\订单&出货&产品信息"

SHIPPING_PLAN_TABLE = _BASE_DIR + r"\发货计划表.xlsx 5.18 .xlsx1.xlsx"
PRODUCT_INFO_TABLE = _BASE_DIR + r"\产品信息表10.17.xlsx"
